plan = "plan/2026-04-01-codex-tui-buddy-reference-parity.md"

[[issue]]
id = "a1"
sequence = 10
title = "重写 Buddy 动态状态 contract 与 tick 基础"
scope = """
把当前 Buddy 从静态反馈模型升级为参考项目同层级的动态运行态基础。包含：整理并落地 deterministic bones、tick、reaction、pet burst、visible 等状态边界；把参考项目中的 `TICK_MS = 500`、idle sequence、bubble/pet 生命周期映射到 Rust 可执行模型；为后续渲染层和接线层提供稳定接口。此 issue 不负责 bottom pane 最终布局、最终 UI snapshot 收口或配置 schema 调整。
"""
touch_points = ["module:codex-tui", "codex-rs/tui/src/buddy/mod.rs", "codex-rs/tui/src/buddy/model.rs", "codex-rs/tui/src/chatwidget/tests/*", "/tmp/claude-code-rev.7F9gvv/src/buddy/companion.ts", "/tmp/claude-code-rev.7F9gvv/src/buddy/types.ts", "/tmp/claude-code-rev.7F9gvv/src/buddy/CompanionSprite.tsx"]
depends_on = []
done_when = "Buddy 模型已能稳定表达 bones、tick、reaction、pet burst 与动画帧选择，且这些状态只由确定性 seed 与本地运行态驱动，不依赖后续接线猜测"
validate_by = "在 `codex-rs` 目录运行 `cargo test -p codex-tui buddy -- --nocapture`，验证 Buddy 模型、状态转移与 tick 相关定向测试"
regress_by = "在放开后续渲染与接线改动前，重新运行 `cargo test -p codex-tui buddy -- --nocapture`，确认动态状态 contract 未被破坏"
regress_timing = "before_fanout"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "已完成 Buddy 动态状态 contract：新增 500ms tick、idle sequence、reaction fade、pet burst 与 teaser 运行态，并通过 `CARGO_TARGET_DIR=/workspace/.cargo-target-buddy-ref cargo test -p codex-tui buddy -- --nocapture` 验证。Execution 期未额外调用能力：当前工作主要是本地 Rust 状态机重构与测试，直接执行比额外能力编排更确定。"

[[issue]]
id = "a2"
sequence = 20
title = "实现动态 sprite、bubble 与 pet hearts 渲染"
scope = """
在 a1 的状态 contract 基础上，为 Buddy 提供持续动态的可视表达。包含：实现 idle/blink/fidget/excited 等帧状态；实现 speech bubble 的显示、淡出与消失；实现 `/buddy pet` 触发的 hearts burst 动效；补齐对应渲染测试与 snapshot。此 issue 不负责 bottom pane 外层布局接线，也不负责配置默认值或 schema。
"""
touch_points = ["module:codex-tui", "codex-rs/tui/src/buddy/mod.rs", "codex-rs/tui/src/buddy/render.rs", "codex-rs/tui/src/**/snapshots/*", "/tmp/claude-code-rev.7F9gvv/src/buddy/CompanionSprite.tsx"]
depends_on = ["a1"]
done_when = "Buddy 在不执行命令时也能以 tick 驱动的方式变化表情或姿态，`/buddy pet` 后会显示持续数个 tick 的 hearts burst，bubble 生命周期可被自动验证并通过 snapshot 审阅"
validate_by = "在 `codex-rs` 目录运行 `cargo test -p codex-tui buddy -- --nocapture`，随后运行 `cargo insta pending-snapshots --manifest-path /workspace/codex-rs/tui/Cargo.toml` 审阅 Buddy 相关待更新 snapshot"
regress_by = "在最终收口前重新运行 Buddy 定向测试与 snapshot 审阅流程，确认动态 sprite、bubble 与 hearts 渲染保持一致"
regress_timing = "after_done"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "已实现动态 sprite、bubble 与 hearts burst，并审阅接受 Buddy snapshot：`cargo insta pending-snapshots --manifest-path /workspace/codex-rs/tui/Cargo.toml` 后执行 `cargo insta accept --manifest-path /workspace/codex-rs/tui/Cargo.toml`。"

[[issue]]
id = "a3"
sequence = 30
title = "完成宽窄屏布局、命令接线与启动提示收口"
scope = """
把动态 Buddy 稳定接入当前 TUI 主链路。包含：宽屏完整 sprite + bubble 与窄屏 compact quip 的布局策略；bottom pane 与 chatwidget 的 redraw 调度；`/buddy show|pet|hide|status` 与新状态模型对齐；按现有通知/消息体系引入 startup teaser 或同等显式提示。此 issue 不把 Buddy 接成模型消息链路，也不引入独立代理人格。
"""
touch_points = ["module:codex-tui", "command:/buddy", "codex-rs/tui/src/bottom_pane/mod.rs", "codex-rs/tui/src/chatwidget.rs", "codex-rs/tui/src/chatwidget/tests/slash_commands.rs", "codex-rs/tui/src/**/snapshots/*", "/tmp/claude-code-rev.7F9gvv/src/buddy/CompanionSprite.tsx", "/tmp/claude-code-rev.7F9gvv/src/buddy/useBuddyNotification.tsx"]
depends_on = ["a1", "a2"]
done_when = "宽屏与窄屏分别采用不同 Buddy 布局策略而不破坏输入区，`/buddy show|pet|hide|status` 的命令反馈与实际 UI 状态一致，且首次启动存在可观察的 Buddy 提示或 teaser"
validate_by = "在 `codex-rs` 目录运行 `cargo test -p codex-tui slash_buddy_show_then_pet_reports_state -- --nocapture`，并补充执行 Buddy 相关 snapshot 与布局测试，验证命令接线、布局与 teaser 提示"
regress_by = "重新运行 Buddy 命令测试、布局相关测试与 snapshot 审阅流程，重点回归 bottom pane、chat composer 与 Buddy 同屏时的显示稳定性"
regress_timing = "after_done"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "已完成宽窄屏动态布局、持续 redraw 调度与启动 teaser 接线；`/buddy show|pet|hide|status` 仍可用，并通过 `CARGO_TARGET_DIR=/workspace/.cargo-target-buddy-ref cargo test -p codex-tui slash_buddy_show_then_pet_reports_state -- --nocapture` 与 Buddy snapshot 测试覆盖。"

[[issue]]
id = "a4"
sequence = 40
title = "收口默认启用配置、schema 与最终验证"
scope = """
收口 Buddy 的默认启用与用户关闭边界，并完成最终验证。包含：保持 `[tui].show_buddy` 默认开启；允许用户通过 `~/.codex/config.toml` 显式关闭；必要时调整配置派生字段与 schema；补齐 `codex-core` 配置测试、Buddy 最终回归测试与 `mise run build` 后的手动验证约定。此 issue 不新增任何可编辑的 rarity、species 或外观骨架配置。
"""
touch_points = ["module:codex-core", "module:codex-tui", "schema:config.schema.json", "codex-rs/core/src/config/types.rs", "codex-rs/core/src/config/mod.rs", "codex-rs/core/src/config/config_tests.rs", "codex-rs/core/config.schema.json", "codex-rs/tui/src/chatwidget.rs"]
depends_on = ["a1", "a3"]
done_when = "Buddy 在默认配置下会出现，用户显式关闭后不会自动显示，配置 schema 与定向测试保持一致，且最终构建产物经手动运行验证能够看到新的动态 Buddy"
validate_by = "如改配置，在 `codex-rs` 目录先运行 `just write-config-schema`，再运行 `cargo test -p codex-core show_buddy -- --nocapture` 与 `cargo test -p codex-tui buddy -- --nocapture`"
regress_by = "在所有 Buddy issue 完成后，重新运行配置相关定向测试、Buddy 定向测试，并执行一次 `mise run build` 后的手动启动检查"
regress_timing = "after_done"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "配置链路无需新增改动：`[tui].show_buddy` 默认开启与显式关闭能力已存在，本轮通过 `CARGO_TARGET_DIR=/workspace/.cargo-target-buddy-ref cargo test -p codex-core show_buddy -- --nocapture` 复核，并以 `mise run build` 验证构建产物已更新到 `/root/.local/bin/codex`。"
