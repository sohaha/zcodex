#!/usr/bin/env bash
#MISE description="打包 Ubuntu 交叉构建出的 Windows 发布产物"
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
repo_root="$(git rev-parse --show-toplevel 2>/dev/null || pwd)"

exec "$repo_root/scripts/package_windows_cross_builds.sh" "$@"
