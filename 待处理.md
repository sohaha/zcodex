那你为什么会说 ztldr 现在是降级到本地 fallback


因为我说的是 ztldr 的原生 daemon 层，不是说 ztldr 整个不能用。

刚验证到的真实状态是：

ztldr status 返回：
structuredFailure: daemon_unavailable
degradedMode.is_degraded: true
但 ztldr structure ... 仍然能跑，返回里明确写了：
source: "local"
message: "daemon unavailable; used local engine"
所以更准确的表述应该是：

ztldr 可用，但 native-tldr daemon 当前没起来，已降级到 local fallback / local engine。

我之前那句如果让你理解成“ztldr 不能用”，那是我表述不准。现在我会继续直接用它的 local 模式排查 workspace_id 这条链路。


---

bash install.sh
==> Updating Codex CLI
==> Detected platform: macOS (Apple Silicon)
curl: (56) The requested URL returned error: 403
Failed to resolve the latest Codex release version.

install.sh 脚本应该不使用 github api 才对