plan = "plan/2026-04-01-codex-tui-buddy-depth.md"

[[issue]]
id = "a1"
sequence = 10
title = "重构 buddy 骨架与运行态模型"
scope = """
基于参考库重新设计 `codex-rs/tui` 的 buddy 数据边界，明确哪些信息由稳定 seed 推导，哪些信息属于运行态或可选持久化态，并把当前极简实现迁移到新的模型上。包含：整理参考库 `companion.ts` / `types.ts` 的关键约束；在 Rust 侧建立 `bones` 与 `state` 的清晰结构；补齐稳定 seed、稀有度/物种推导和 pet 反应状态的定向测试。此 issue 不负责底部渲染升级、UI snapshot 或配置 schema 收口。
"""
touch_points = ["module:codex-tui", "codex-rs/tui/src/buddy.rs", "codex-rs/tui/src/buddy/*", "codex-rs/tui/src/chatwidget.rs", "codex-rs/tui/src/chatwidget/tests/*", "/tmp/claude-code-rev.7F9gvv/src/buddy/companion.ts", "/tmp/claude-code-rev.7F9gvv/src/buddy/types.ts"]
depends_on = []
done_when = "Rust TUI 中的 buddy 已清晰拆分为稳定骨架与运行态状态，骨架属性只由 seed 决定且不可被用户配置伪造，pet/show/hide/status 所需状态转移都有可复现的测试覆盖"
validate_by = "在 `codex-rs` 目录运行 `cargo nextest run -p codex-tui buddy`；若本地未安装 `cargo-nextest`，则退化为 `cargo test -p codex-tui buddy -- --nocapture`"
regress_by = "在放开渲染层和配置层改动前，重新运行 buddy 模型与状态转移的定向测试，确认骨架推导和运行态边界未被后续接线破坏"
regress_timing = "before_fanout"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "已完成 bones/state 重构，吸收参考实现中的 rarity/species/eye/hat/shiny/stats 骨架原则，并通过 `cargo test -p codex-tui buddy -- --nocapture` 验证状态转移。当前轮次未使用额外 capability：主任务是本地 Rust 重构、snapshot 更新与定向测试，直接执行比额外技能编排更确定。"

[[issue]]
id = "a2"
sequence = 20
title = "升级 buddy 渲染与窄宽屏布局"
scope = """
在 a1 的新模型基础上，为 buddy 提供比当前单行文本更完整的 TUI 可视表达。包含：抽离独立的 buddy 渲染模块；为窄屏提供紧凑形态、为宽屏提供扩展形态；把 buddy 稳定接入底部区域而不破坏输入区、状态栏和 pending preview 布局；补齐必要的 snapshot 与相关渲染测试。此 issue 不负责 slash 命令语义扩展或配置 schema 调整。
"""
touch_points = ["module:codex-tui", "codex-rs/tui/src/buddy.rs", "codex-rs/tui/src/buddy/*", "codex-rs/tui/src/bottom_pane/mod.rs", "codex-rs/tui/src/chatwidget.rs", "codex-rs/tui/src/chatwidget/tests/*", "codex-rs/tui/src/snapshots/*"]
depends_on = ["a1"]
done_when = "Buddy 在常见终端宽度下都能稳定显示合适形态，底部布局不被挤坏，且对应 snapshot 与渲染测试已更新到可审阅状态"
validate_by = "在 `codex-rs` 目录运行 `cargo nextest run -p codex-tui`；若产生新的 UI snapshot，再用 `cargo insta pending-snapshots -p codex-tui` 审阅待接受快照"
regress_by = "重新运行 `codex-tui` 的 buddy 相关测试与 snapshot 流程，重点回归 bottom pane、chat composer、pending preview 与 buddy 同屏时的布局稳定性"
regress_timing = "after_done"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "已抽离 `tui/src/buddy/model.rs` 与 `tui/src/buddy/render.rs`，补齐宽屏/窄屏渲染与 snapshot，并通过 `cargo test -p codex-tui buddy -- --nocapture` 回归。"

[[issue]]
id = "a3"
sequence = 30
title = "升级 `/buddy` 交互反馈与状态可解释性"
scope = """
在 a1 和 a2 的基础上，提升 `/buddy show|pet|hide|status` 的用户反馈质量。包含：让 pet/show/hide 不只返回命令文案，还会驱动可见状态反馈；完善 `/buddy status` 的状态说明，使用户能看懂当前 buddy 的可见性、最近交互和反应状态；补齐相关 slash command 与界面反馈测试。此 issue 不把 buddy 扩展成模型提示注入链路、聊天代理人格或独立发言者。
"""
touch_points = ["module:codex-tui", "command:/buddy", "codex-rs/tui/src/slash_command.rs", "codex-rs/tui/src/chatwidget.rs", "codex-rs/tui/src/buddy.rs", "codex-rs/tui/src/buddy/*", "codex-rs/tui/src/chatwidget/tests/*", "codex-rs/tui/src/snapshots/*"]
depends_on = ["a1", "a2"]
done_when = "`/buddy pet|show|hide|status` 都能产生与当前 buddy 状态一致的可见反馈，`/buddy status` 输出对用户可解释，且对应命令分发与 UI 表现都有定向测试覆盖"
validate_by = "在 `codex-rs` 目录运行 `cargo nextest run -p codex-tui slash_commands`，并补充执行 buddy 相关渲染/交互测试以确认 pet 与显隐反馈能被自动验证"
regress_by = "在所有 buddy 相关改动收口后，重新运行 `/buddy` 命令测试、交互测试与关联 snapshot，确认命令列表、分发逻辑和视觉反馈保持一致"
regress_timing = "after_done"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "已增强 `/buddy show|pet|hide|status` 反馈，启动默认显示改为静默接线，命令与状态说明由 `cargo test -p codex-tui buddy -- --nocapture` 覆盖。"

[[issue]]
id = "a4"
sequence = 40
title = "收口 buddy 默认显示配置与持久化边界"
scope = """
为最终版 buddy 系统收口用户可控配置与最小必要持久化边界。包含：将 buddy 默认显示设为开启；允许用户通过 `~/.codex/config.toml` 的 `[tui]` 配置关闭；同步 `ConfigToml` 派生字段、配置测试和 `config.schema.json`；若需要保留最小运行态偏好，只允许保留不影响骨架一致性的状态。此 issue 不新增可编辑的 rarity、species 或外观骨架配置。
"""
touch_points = ["module:codex-core", "module:codex-tui", "schema:config.schema.json", "codex-rs/core/src/config/types.rs", "codex-rs/core/src/config/mod.rs", "codex-rs/core/src/config/config_tests.rs", "codex-rs/core/config.schema.json", "codex-rs/tui/src/chatwidget.rs", "codex-rs/tui/src/chatwidget/tests/*"]
depends_on = ["a1", "a2"]
done_when = "Buddy 在默认配置下会显示，用户可通过 `~/.codex/config.toml` 关闭，配置 schema 与定向测试已同步，且任何持久化字段都不会允许用户伪造骨架属性"
validate_by = "在 `codex-rs` 目录先运行 `just write-config-schema`，再运行 `cargo nextest run -p codex-core config_tests` 与 `cargo nextest run -p codex-tui buddy`；若本地未安装 `cargo-nextest`，则退化为等价的 `cargo test` 命令"
regress_by = "在最终收尾前重新运行配置相关定向测试与 buddy 相关 TUI 测试，确认默认显示、显式关闭和 UI 接线在最终实现下保持一致"
regress_timing = "after_done"
status = "done"
validate_status = "passed"
regress_status = "passed"
notes = "已保持 `[tui].show_buddy` 默认开启、显式关闭可用，并执行 `just write-config-schema` 与 `cargo test -p codex-core show_buddy -- --nocapture` 验证配置层。"
