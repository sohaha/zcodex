plan = ".agents/plan/2026-04-06-tmux-resize-render.md"

[[issue]]
id = "a1"
sequence = 10
title = "稳定 resize 后 inline viewport 的定位与重绘"
scope = """
修改 `codex-rs/tui/src/tui.rs` 中与 resize 相关的 viewport 重定位路径，去掉对不可靠光标位移启发式的单点依赖，并补充覆盖“resize 发生但 cursor 反馈不可信或未变化”场景的回归测试。必要时更新与该行为直接相关的快照或测试辅助代码。

不包括 buddy/宠物功能改动，不包括非 resize 触发的其他渲染问题，也不包括对 tmux 做独立兼容层或大规模 TUI 重构。
"""
touch_points = ["module:tui", "codex-rs/tui/src/tui.rs", "codex-rs/tui/tests", "codex-rs/tui/src"]
depends_on = []
done_when = "resize 后 inline viewport 的位置与可见内容在 tmux 和普通终端路径下都保持稳定，不再依赖单一 cursor delta 启发式；仓库中新增或更新了覆盖该回归的测试，并通过 `codex-tui` 的对应验证。"
validate_by = "在 `codex-rs` 目录运行 `cargo nextest run -p codex-tui`；若本地缺少 nextest，则运行 `cargo test -p codex-tui`。"
regress_by = "none"
regress_timing = "after_done"
status = "in_progress"
validate_status = "failed"
regress_status = "not_needed"
notes = "已在 `codex-rs/tui/src/tui.rs` 实现 inline viewport resize 时的底部重对齐，并新增两条单元测试。`cargo test -p codex-tui inline_resize` 已通过；完整 `cargo nextest run -p codex-tui` 因仓库中现有 snapshot 失败而中断，当前可见失败点为 `bottom_pane::chat_composer::tests::zellij_empty_composer_snapshot` 与两个 `mcp_server_elicitation` approval form snapshot，未见与本文件改动直接相关的失败。另已在 tmux 3.3a 中做自动化 resize 复核：从 120x30 缩到 90x20 再放回 120x30，进程持续存活，header、buddy、输入区与底部状态行均保持可见。"
