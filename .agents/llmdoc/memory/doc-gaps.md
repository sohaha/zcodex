# 文档缺口

## 当前缺口
- 缺少面向 `tui` 的细化文档：尚未把聊天区、底栏、approval、buddy、plugins 等高频模块拆成独立架构文档。
- 缺少 app-server v2 / protocol 演进的专门路由文档，目前仍需直接回读 README 与协议类型。
- 缺少 release / cross-build / 签名流水线的专门 guide，当前信息主要散落在 `docs/install.md`。
- 缺少 `.agents/plan`、`.agents/issues` 与 `llmdoc` 的协作约定说明，后续可以补一份“任务产物与稳定文档边界”指南。

## 下一步
- 未来首次进入上述子系统做非简单改动时，顺手把对应经验沉淀为新的 `architecture/` 或 `guides/` 文档。
