# Legacy TUI Style Notes

The active TUI implementation now lives in `codex-rs/tui_app_server`.

For current styling guidance, follow the file-local conventions in
`codex-rs/tui_app_server` and the shared TUI rules in `AGENTS.md`.
