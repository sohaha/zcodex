interface:
  display_name: "同步 OpenAI Codex 上游"
  short_description: "在独立 worktree 同步 openai/codex，并审查本地分叉特性"
  default_prompt: "Use $sync-openai-codex-pr to sync openai/codex main into the current branch, preserve local fork behavior, audit local-only features before merging back, and update the sync baseline."

policy:
  allow_implicit_invocation: true
