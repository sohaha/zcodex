interface:
  display_name: "同步 CTF 上游"
  short_description: "按最小范围把 codex-session-patcher 的 Codex CTF 逻辑同步到本仓"
  default_prompt: "Use $sync-codex-session-patcher to compare the local `codex ctf` implementation with the upstream `ryfineZ/codex-session-patcher` repo, port the minimal necessary Codex-specific behavior, and update the recorded sync baseline."

policy:
  allow_implicit_invocation: true
