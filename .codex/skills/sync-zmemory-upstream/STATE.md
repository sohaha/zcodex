# sync-zmemory-upstream state

- upstream_repo: https://cnb.cool/zls_nmtx/sohaha/nocturne_memory
- upstream_ref: a574c2d92bcfe377441e35d27f883fe1cb39e1e1
- last_synced_hash: selective-sync-compat-adapter-web-reuse-validated
- last_synced_at_utc: 2026-04-08T10:43:44Z
- notes: local selective sync now covers namespace-aware runtime/db contracts, review diff service contracts, and a thin upstream-compatible HTTP adapter for real upstream web reuse without widening `codex-zmemory` into a second backend. Parity surface is `codex-rs/zmemory/src/compat/*` plus `codex zmemory serve-compat`, which now serves both `/api/*` and proxy-stripped `/*` browse/review/maintenance routes against the same dbPath/namespace resolution as CLI/core. Review queue enumeration filters out orphaned nodes that cannot build diffs, and minimal review mutations are implemented locally via audit markers: approve/clear-all emit `review-approve`, rollback replays inverse local audit semantics and emits `review-rollback`. Validation used `cargo nextest run -p codex-zmemory`, `cargo nextest run -p codex-cli --test zmemory`, `just fix -p codex-zmemory`, and a real upstream frontend run (`/workspace/.tmp/nocturne_memory`, frontend `127.0.0.1:3000`, compat `127.0.0.1:8000`) against `system://workspace.dbPath=/workspace/.tmp/zmemory-compat-a5/home/zmemory/projects/workspace-c52ddf65534b/zmemory.db`, covering `/review` approve/rollback, `/memory`, `/governance-lab`, and `/maintenance`. Remaining known gap: `/browse/glossary` POST/DELETE is still not implemented, so memory-browser keyword writes are not yet parity-complete.
