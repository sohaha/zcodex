interface:
  display_name: "同步 Zmemory 上游"
  short_description: "按最小范围把 nocturne_memory 的相关变更同步到 codex-zmemory"
  default_prompt: "Use $sync-zmemory-upstream to compare codex-zmemory with the upstream nocturne_memory reference, port the minimal necessary memory behavior, and update the recorded sync baseline."

policy:
  allow_implicit_invocation: true
